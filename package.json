{
  "name": "uainotch",
  "version": "1.0.0",
  "description": "UaiNotch Initial Project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server --baseDir=dist",
    "start": "concurrently \"npm run watch\" \"npm run server\"",
    "compile": "tsc",
    "watch": "tsc -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0",
    "lite-server": "^2.6.1",
    "tsc": "^2.0.4",
    "typescript": "^5.0.2"
  }
}
